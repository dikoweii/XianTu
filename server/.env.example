# 复制为 `server/.env` 并按需修改

# development | production
ENVIRONMENT=development

# JWT：生产环境务必替换为随机值（例如 `openssl rand -hex 32`）
SECRET_KEY=dev-secret-change-me

# 数据库连接串（默认使用 SQLite，开箱即用）
DDCT_DB_URL=sqlite://db.sqlite3

# MySQL 示例：
# DDCT_DB_URL=mysql://user:password@127.0.0.1:3306/ddct?charset=utf8mb4

# Cloudflare Turnstile（生产环境建议开启）
# TURNSTILE_SECRET_KEY=your-turnstile-secret-key
